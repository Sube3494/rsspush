# RSS推送插件配置文件

# 全局配置
polling:
  interval: 30  # 轮询间隔（分钟）
  enabled: true

push:
  max_items_per_push: 1  # 单次推送最大条目数
  batch_interval: 3      # 批量推送间隔（秒）
  
deduplication:
  enabled: true
  time_window: 7  # 去重时间窗口（天）

template:
  default: |
    🔔 {name}
    
    📰 {title}
    🕐 {pubDate}
    
    📝 {description}
    
    🔗 {link}

rsshub:
  default_instance: "https://rsshub.app"

# P2功能配置
silent_hours:
  enabled: false
  start: "23:00"
  end: "07:00"

rate_limit:
  enabled: false
  max_pushes_per_hour: 10

